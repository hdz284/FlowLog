.in 
.decl Edge(src: number, dest: number) 
.input arc.facts
.decl NullEdge(src: number, dest: number) 
.input arc.facts

.printsize 
.decl tc(src: number, dest: number)

.rule
tc(x, y) :- NullEdge(x, y).
tc(x / 2 + y, z * y + 1) :- tc(x, y), Edge(y, z).
